//Laços de Repetição slide atv.2 p.155

const prompt = require('prompt-sync')();

let listadecompras = ["Arroz", "Feijão", "Macarrão", "Carne"];

for (let i = 0; i < listadecompras.length; i++) {
    console.log(`item: ${listadecompras[i]}`);
}